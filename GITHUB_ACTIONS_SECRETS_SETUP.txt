B2BGearVia GitHub Actions and Deployment Notes

This transition repository currently has no active GitHub Actions deployment workflow and no configured Git remote.

Do not add production credentials, OAuth secrets, payment keys, SSH keys, or server hostnames to this file. Runtime values belong in the target server environment or in an approved secret manager after the B2BGearVia deployment process is finalized.

Current local-only defaults:

- Application runtime namespace: b2bgearvia
- User-facing product name: B2BGearVia
- Default MySQL database: b2bgearvia
- Runtime installation path: /opt/b2bgearvia
- Frontend development origin: http://localhost:5174
- Backend development origin: http://localhost:8081
- Admin development origin: http://localhost:19091

Before any future workflow is enabled, create a new B2BGearVia-specific deployment design and review it separately. Do not reuse archived public-domain, EC2, TLS-renewal, or original SaaS deployment automation.
