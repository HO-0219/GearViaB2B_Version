package com.teamproject.deployment.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface DeploymentSettingsRepository extends JpaRepository<DeploymentSettings, Long> {
}
